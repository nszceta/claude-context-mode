{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "context-mode": {
      "type": "local",
      "command": ["npx", "-y", "context-mode"],
      "enabled": true,
      "timeout": 10000
    }
  }
}
