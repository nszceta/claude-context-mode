{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "context-mode": {
      "type": "local",
      "command": ["node", "${CONFIG_DIR}/start.mjs"],
      "enabled": true,
      "environment": {
        "OPENCODE_PROJECT_DIR": "${PWD}"
      }
    }
  }
}
